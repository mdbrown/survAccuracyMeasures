

getEstimates <- function(data, 
                         cutpoint,  
                         measures,
                         predict.time,
                         CalVar, cutoff.type = "none", cutoffN = 100, subcohort=FALSE)
{
  
}